interface:
  display_name: "Vesper Plugin Runtime"
  short_description: "Catalog, plan, scope, and playback lifecycle"
  default_prompt: "Use $vesper-plugin-runtime to change Vesper plugin runtime lifecycle safely."
policy:
  allow_implicit_invocation: true
