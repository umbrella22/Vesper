interface:
  display_name: "Vesper Plugin Development"
  short_description: "End-to-end plugin implementation workflow"
  default_prompt: "Use $vesper-plugin-development to turn this requirement into a Vesper plugin safely."
policy:
  allow_implicit_invocation: true
