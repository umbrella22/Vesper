interface:
  display_name: "Vesper Architecture Memory"
  short_description: "Vesper runtime and boundary memory"
  default_prompt: "Use $vesper-architecture-memory to place this feature in the right Vesper layer."
policy:
  allow_implicit_invocation: true
