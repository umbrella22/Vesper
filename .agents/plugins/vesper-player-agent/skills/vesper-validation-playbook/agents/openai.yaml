interface:
  display_name: "Vesper Validation Playbook"
  short_description: "Vesper verification command routing"
  default_prompt: "Use $vesper-validation-playbook to choose checks for this Vesper patch."
policy:
  allow_implicit_invocation: true
