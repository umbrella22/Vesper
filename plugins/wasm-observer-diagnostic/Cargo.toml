[package]
name = "vesper-plugin-wasm-observer-diagnostic"
version = "0.5.0"
edition = "2024"
rust-version = "1.98"
license = "Apache-2.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
player-plugin-wasm = { path = "../../crates/plugin/player-plugin-wasm" }

[profile.release]
panic = "abort"

[workspace]
