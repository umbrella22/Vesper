# Vesper Performance Diagnostics

This optional Native `BenchmarkSink` aggregates bounded UI frame samples and
native playback pressure signals. Its diagnosis describes correlation between
an active host overlay and UI frame pressure; it does not establish causation.

The plugin does not perform network requests or upload reports. Hosts own
activation, persistence, sharing, and any remote configuration.
